const calculator = document.getElementById('calcualtor');
const keys = document.querySelector('.calculator--keys');
const 
keys.addEventListener('click', e => {
  if (e.target.matches('button'){
    alert('I was clicked')
  })
})
